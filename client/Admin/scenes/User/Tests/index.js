import React from 'react'
import PropTypes from 'prop-types'

const Tests = () => {
    return (
        <div>
        </div>
    )
}

Tests.propTypes = {
    tests: PropTypes.array
}

export default Tests